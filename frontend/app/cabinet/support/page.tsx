import { SupportApp } from "@/components/CabinetPages";

export const metadata = {
  title: "Поддержка | Arvexo Connect",
  description: "Поддержка пользователей Arvexo Connect.",
  robots: { index: false, follow: false }
};

export default function CabinetSupportPage() {
  return <SupportApp />;
}
